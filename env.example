# Azure AD App Registration — Client ID (public, safe for client-side)
VITE_MSAL_CLIENT_ID=

# Azure AD Tenant ID (public, safe for client-side)
VITE_MSAL_TENANT_ID=

# Azure DevOps PAT — set in api/local.settings.json (local) or Azure Static Web App Application Settings (production)
# ADO_PAT=
